bind = "0.0.0.0:8000"  # Bind to the specified address and port
worker_class = "gthread"  # Use the gthread worker class
workers = 4  # Number of worker processes
threads = 2  # Number of threads per worker
